# Benchmarks

The benchmark document now lives with the rest of the documentation, at
**<https://sdns.dev/docs/reference/benchmarks/>**, method, environment, each
contender's configuration, the raw tables, the caveats, and the commands to
reproduce it.

Its source is [`docs/_docs/reference/benchmarks.md`](docs/_docs/reference/benchmarks.md),
still in this repository and still versioned with the code that was measured.
It moved rather than being copied, so there is one set of numbers rather than
two that drift.
