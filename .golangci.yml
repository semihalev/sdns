# golangci-lint configuration
# https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m

linters:
  disable:
    - errcheck
    - staticcheck
    - goconst
    - gosec
  enable:
    - govet
    - ineffassign
    - unused
    - misspell
    - unconvert
    - gocritic
