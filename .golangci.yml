# golangci-lint configuration
# https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m

linters:
  disable:
    - errcheck
  enable:
    - govet
    - ineffassign
    - unused
    - misspell
    - unconvert
    - gocritic
    - gosec
    - staticcheck
