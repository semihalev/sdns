_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/
.bundle/
# Gemfile.lock is committed: CI builds with the same locked toolchain the
# site is developed against, rather than whatever a resolver picks that day.
_config.release.yml
